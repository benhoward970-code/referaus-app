// Sentry instrumentation — disabled until @sentry/nextjs adds Next.js 16 support.
// Config files (sentry.*.config.ts) are ready to go when that lands.
export async function register() {
  // no-op for now
}
