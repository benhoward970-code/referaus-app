// Sentry server config — ready for when @sentry/nextjs supports Next.js 16.
// import * as Sentry from "@sentry/nextjs";
// Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 0.1, enabled: process.env.NODE_ENV === "production" });
export {};
